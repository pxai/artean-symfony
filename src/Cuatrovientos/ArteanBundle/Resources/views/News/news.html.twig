<tr class="{{ news.status|pageStatusColor }}">
     <td>{{ news.id }}</td> 
     <td>{{ news.title }}</td>
    <td>
        {% if news.who == 1 %}
            Artean
        {% else %}
            La empresa
        {% endif %}
    </td>
    {#% if is_granted('ROLE_ADMIN') %#}
			<td>
                <a href="{{ path('cuatrovientos_artean_news_detail', {'id': news.id }) }}" class="btn btn-primary"><i class="fa fa-search-plus" aria-hidden="true"></i></a>
                <a href="{{ path('cuatrovientos_artean_news_update', {'id': news.id }) }}" class="btn btn-warning"><i class="fa fa-edit" aria-hidden="true"></i> </a>
                <a href="{{ path('cuatrovientos_artean_news_delete', {'id': news.id }) }}" class="btn btn-danger"><i class="fa fa-trash-o" aria-hidden="true"></i> </a>

		    </td>
		{#% endif % #}
<//tr>
