<tr>
     <td>{{ studentCourse.id }}</td>
    <td>{{ studentCourse.applicant.surname }}, {{ studentCourse.applicant.name }}</td>
    <td>{{ studentCourse.applicant.email }}</td>
    <td>{{ studentCourse.applicant.mobile }}</td>
    {#% if is_granted('ROLE_ADMIN') %#}
    <td>
        <a href="{{ path('cuatrovientos_artean_studentcourse_detail', {'id': studentCourse.id }) }}" class="btn btn-primary"><i class="fa fa-search-plus" aria-hidden="true"></i></a>
        <a href="{{ path('cuatrovientos_artean_studentcourse_update', {'id': studentCourse.id }) }}" class="btn btn-warning"><i class="fa fa-edit" aria-hidden="true"></i> </a>
        <a href="{{ path('cuatrovientos_artean_studentcourse_delete', {'id': studentCourse.id }) }}" class="btn btn-danger"><i class="fa fa-trash-o" aria-hidden="true"></i> </a>
    </td>

		{#% endif % #}
</tr>
