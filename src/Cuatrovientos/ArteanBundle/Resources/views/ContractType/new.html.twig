{% extends "CuatrovientosArteanBundle:Default:layout.html.twig" %}
{% block title %}Artean :: Tipos de contrato : nueva{% endblock %}
{% block body %}
   <h2><i class="fa fa-industry" aria-hidden="true"></i> Tipos de contrato</h2>

   <a href="{{ path('cuatrovientos_artean_contracttype') }}" class="btn">
      <i class="fa fa-list-alt" aria-hidden="true"></i> Ver todos
   </a>

   <h3><i class="fa fa-plus" aria-hidden="true"></i> Nuevo tipos de contrato</h3>
<form method="post" action="{{ path('cuatrovientos_artean_contracttype_new_save') }}">
   {{ form_errors(form) }}
   <div class="form-group">
      {{ form_label(form.name) }}
      {{ form_errors(form.name) }}
      {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
   </div>
   <div class="form-group">
      {{ form_row(form._token) }}
      {{ form_widget(form.save, {'attr': {'class': 'form-control'}}) }}
   </div>
</form>

{% endblock %}