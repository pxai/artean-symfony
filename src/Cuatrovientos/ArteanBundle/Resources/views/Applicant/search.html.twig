<div class="modal-dialog" role="document">
   <div class="modal-content">
      <form method="post" action="{{ path('cuatrovientos_artean_applicant_admin_search') }}" class="arteanform">
      <div class="modal-header">

   <a href="{{ path('cuatrovientos_artean_applicant_admin') }}" class="btn">
      <i class="fa fa-list-alt" aria-hidden="true"></i> Ver todos
   </a>

   <h3><i class="fa fa-search" aria-hidden="true"></i> Buscar candidatos</h3>
         <h4 class="modal-title" id="exampleModalLabel">Buscar candidatos</h4>
      </div>
      <div class="modal-body">

   {{ form_errors(form) }}
   <div class="form-group">
      {{ form_label(form.name) }}
      {{ form_errors(form.name) }}
      {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
   </div>
         <div class="form-group">
            {{ form_label(form.surname) }}
            {{ form_errors(form.surname) }}
            {{ form_widget(form.surname, {'attr': {'class': 'form-control'}}) }}
         </div>
      {{ form_row(form._token) }}

      </div>
      <div class="modal-footer">
         <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
         <button type="submit" class="btn btn-primary">Buscar candidatos</button>
      </div>
      </form>
   </div>
</div>